# Ignore Git metadata
.git
.gitignore

# Ignore Dockerfiles in subfolders (not needed)
Dockerfile*
Dockerfile.*

# Ignore local build artifacts
*.o
*.exe

# Anything else that shouldn’t go into the container
